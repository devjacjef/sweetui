// Shared type interfaces

/**
 * Defines props for `sweetui` components.
 *
 * @property {boolean} [sweet] - Controls whether sweet styling is applied.
 * @default true
 */
export interface SweetBase {
  sweet?: boolean;
}
